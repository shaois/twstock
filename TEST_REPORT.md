# V93 驗證報告

交付快取資料日：2026-09-21。驗證完成日：2026-09-23。

## 已完成

- 使用 requirements.txt 建立全新隔離依賴，Python 3.12 / Node.js 24。
- 以200支股票及0050既有行情完整執行 rebuild_cache.py：199支可預測、產生199份同日期 AI 資料卡。沒有呼叫 FinMind 或 AI。
- 81項 Python 測試全數通過，涵蓋模型、時間切割、資料更新、機率校準、排名、Pages 部署腳本、API 代理與新契約。
- V93 JavaScript 整合測試通過：199份真實快取；前5筆模擬回答；4種決策均可表達；錯誤不轉成等待；未知引用明示警告；長度截斷拒絕；不變更預測與排名。
- 跨版本、缺資料在送出上游前中止；重複點擊不增加呼叫；切換股票後舊回應不覆蓋畫面；診斷遮蔽測試金鑰。
- HTTP 400、401、404、408、429、500、502、503、504 每個請求只執行一次上游呼叫；非 JSON 上游回應明確回傳502。
- 觀察榜與原始日榜切換、歷史不足、同日重跑不重複累積、版本／股票池變更重置、完整精度排序、跨實驗名次不直接比較均通過測試。
- 使用 computer-use 實際開啟本機網站，確認快取199/200、觀察榜、1/5資料日顯示。
- 在明確標示「離線模擬測試」的網站，用假金鑰經真實前端與 API 路由走完整分析流程，畫面顯示 AI 主觀意見、引用程式資料卡、假設情境及匯出診斷按鈕；已檢查實際版面。

## 不代表已驗證的事項

- 沒有使用真實帳號金鑰向 Groq / NVIDIA 執行推論。因此帳號權限、供應商實際回應品質、限流與真實延遲尚未驗證。
- 沒有替使用者推送儲存庫或更新線上 Render / Pages。
- 格式與來源存在檢查不是語意正確性驗證；AI 意見明確標為未核實。
- 觀察榜採最多5資料日平均，是降低單日變化影響的展示規則，尚未證明換手率或交易績效改善。首次新實驗只有1日，不虛構舊實驗歷史。
- 未證明此模型能獲利、優於0050，或前5名必須出現可買股票。沒有為產生買進答案而降低門檻。

## 重現

`python verify_release.py`：完整離線測試；需要 Node.js。

`python tests/serve_ui_fixture.py`：只綁定127.0.0.1、由系統分配埠的離線 UI fixture。所有 AI 回應均在本機模擬，請勿作投資判斷。正式入口是 `python start.py`，不會載入 fixture。
